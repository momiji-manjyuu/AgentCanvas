Phase 1: schema v0.2.0 migration and sample regeneration completed; gates passed with pnpm typecheck, pnpm lint, pnpm test.
Phase 2: checked save, content hashing, disk merge preservation, and manual reload completed; gates passed with pnpm typecheck, pnpm lint, pnpm test.
Phase 3: chokidar-based diagram watcher, self-echo suppression, IPC push events, and renderer external-change merge completed; gates passed with pnpm typecheck, pnpm lint, pnpm test.
Phase 4: session activity feed, external/local activity recording, and proposal highlight links completed; gates passed with pnpm typecheck, pnpm lint, pnpm test.
Phase 5: review notes, threaded comments, agent comment MCP tool, and English MCP apply rejection completed; gates passed with pnpm typecheck, pnpm lint, pnpm test.
Phase 6: partial proposal apply, op subset preview, applied op tracking, and readable op labels completed; gates passed with pnpm typecheck, pnpm lint, pnpm test.
Phase 7: renderer i18n dictionaries, language toggle with localStorage, localized UI/toasts/confirms, and diagram timestamp display completed; gates passed with pnpm typecheck, pnpm lint, pnpm test.
Phase 8: MCP setup info IPC/UI, Claude command and JSON copy actions, mcp-server tsup bundle, and packaged extraResource wiring completed; gates passed with pnpm typecheck, pnpm lint, pnpm test.
Phase 9: PNG and standalone HTML export via save dialog completed; gates passed with pnpm typecheck, pnpm lint, pnpm test.
Phase 10: dual-actor collaboration tests, README v0.2 updates, final build, package:dir, and packaged MCP bundle smoke check completed; gates passed with pnpm typecheck, pnpm lint, pnpm test, pnpm build, pnpm package:dir.
